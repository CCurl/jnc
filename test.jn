int xxx;
int Abc[1000000];
char yyy;
char Def[256];

def sub1
    Y = M * X + B;
end

def Bye A=1; B=0; sys; end

def main
    C="hello!"; D=6; A=4; B=0; sys;
    C = 12;
    A = 1 + Abc - 3;
    if (C == 0)
        A=4; B=0; C="/0!\n"; D=4; sys;
        return;
    end
    B = A * B / C;
    I = I + 1;
    M=13; X=2; B=100; sub1;
    xxx = A+3+yyy;
    D++;
    xxx--;
    X = Y++;
    Z = Y--;
    Y = xxx++;
    Z = xxx--;
    C = &Def;
    C=" ... "; D=5; A=4; B=0; sys;
    D = 500*1000*1000;
    // Bye;
    while (D) D--; end
    C="bye\n"; D=4; A=4; B=0; sys;
end
