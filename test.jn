int xxx;
int Abc[1000000];
char yyy;
char Def[256];

// Sets D = the length of C
def strLen 
    D=C;
    while (*D) D++; end
    D = D-C;
end

// prints the string at C
def print strLen; B=0; A=4; sys; end

def Bye A=1; B=0; sys; end
def MXB Y = M * X + B; end

def BM
    +L;
    C = "Start ... "; print;
    D = 1000*1000*1000;
    while (D) D--; end
    C="end\n"; print;
    -L;
end

def main
    C="hello! "; print;
    BM;
    C = 1;
    A = 1 + Abc - 3;
    if (C == 0)
        C=" C==0!\n"; print;
        return;
    end
    B = A * B / C;
    I = I + 1;
    M=13; X=2; B=100; MXB;
    xxx = A+3+yyy;
    D++;
    xxx--;
    X = Y++;
    Z = Y--;
    Y = xxx++;
    Z = xxx--;
    C = &Def;
end
